[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node apps/scraper-backend/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
